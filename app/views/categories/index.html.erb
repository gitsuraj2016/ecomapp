<div class="container-fluid">
<p id="notice"><%= notice %></p>

<h1>Listing Categories</h1>

<table class="table table-bordred table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Active</th>
      <th colspan="3" style="padding-left:64px;">Action</th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= category.description %></td>
        <td><%= image_tag(category.image_url, class: 'list_image', width: "30px" ,heigth: "30px") %></td>
        <td><%= category.active %></td>
        <td><%= link_to 'Show', category,{:class => "btn btn-success"} %>
        <%= link_to 'Edit', edit_category_path(category),{:class => "btn btn-info"} %>
        <%= link_to 'Destroy', category,:class => "btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Category', new_category_path,{:class => "btn btn-primary"} %>
</div>
